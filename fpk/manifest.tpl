appname               = CyanNVR
version               = @VERSION@
display_name          = CyanNVR
desc                  = CyanNVR 是一款轻量级网络视频录像机（NVR）。<br><br>支持 ONVIF 摄像头自动发现、实时预览、多画面分割、录像回放、AI 目标检测与事件管理。安装时可自定义服务端口，安装后直接从飞牛桌面打开。默认账号 admin / admin123。
platform              = x86
source                = thirdparty
maintainer            = CyanNVR
maintainer_url        = https://github.com/upcyan/SimpleNVR
distributor           = CyanNVR
distributor_url       = https://github.com/upcyan/SimpleNVR
os_min_version        = 1.0.0
service_port          = 18182
checkport             = false
ctl_stop              = true
desktop_uidir         = ui
desktop_applaunchname = CyanNVR.main
changelog             = v1.6.1<br>- 修复 CPU 占用异常高：根因是进程无信号处理，fnOS 停止/升级时 Go 被直接击杀、defer 不执行，每次重启泄漏一整套录像/直播/快照 ffmpeg 孤儿（单个约30% CPU，连续升级后堆积至负载20+）<br>- 新增优雅停机：捕获 SIGTERM/SIGINT 后正常退出，rec.Stop() 回收全部 ffmpeg 子进程<br>- stop 脚本兜底清扫：无论何种退出方式，都会回收本应用数据目录下 PPID=1 的孤儿 ffmpeg<br><br>v1.6.0<br>- 用户UID改为纯数字：创建时自动分配（现有最大值+1），老用户升级时按创建顺序自动回填，列表/对话框/CLI 均展示 UID（如 UID 1）；身份判定仍走不可变 id，改名不影响任何归属<br>- 「改名」「修改密码」按钮移到对应输入框正下方（原先在弹窗底部易被忽略），管理员与普通用户均可改名（服务端无角色限制）<br>- 设置页底部新增版本自检行（看到「已加载最新界面」= 浏览器缓存已更新；看不到该行说明还在读旧缓存，强刷或重开窗口即可）<br>- 彻底禁用前端缓存：Service Worker 改为自毁模式（安装即注销并清空预缓存），后端为全部静态响应加 Cache-Control: no-cache——升级后重新打开一次即为最新界面，F5 不再吃到旧缓存<br><br>v1.5.0<br>- 推理后端 auto 拆分为两档：「自动 · 预设」（按 cuda→rocm→openvino→directml→cpu 顺序取第一个可用）与「自动 · 实测」（启动逐个 EP 跑微型基准择优）<br>- 实测记分卡：平均毫秒（择优排序依据）+ 峰值（稳定性）+ 首帧耗时（冷启动代价）+ 与 CPU 基准的输出偏差 Δ（精度资格门，算错的直接出局）+ 内存增量 RSS；设置页展示完整排名与指标；TensorRT 因首跑需数十秒建引擎不参与启动实测，仍可手动选择<br>- 用户管理重构：编辑对话框加入独立「改名」「修改密码」按钮（即点即生效），展示不可变用户ID（列表带 ID 短码；改名不改变身份识别），角色选择即时保存<br>- 推理后端实测档新增「实测结果」卡片：按均值名次列出各后端 均值/峰值/首帧/精度Δ/内存增量<br>- 循环覆盖改为输入框+下拉箭头融合控件（直接可输1-3650天，箭头呼出快捷档），移除独立快捷档位行<br><br>v1.4.0<br>- 推理后端支持手动切换：设置页「推理后端」可选 自动/CPU/CUDA/TensorRT/ROCm/OpenVINO/DirectML（保存后重启应用生效），标签显示当前实际生效的后端；auto 默认按可用性挑最快（本机有 CUDA 即 GPU）<br>- 默认检测模型升级为 yolo11n（同为 nano 速度、更准、误报更少）：出厂打包、启动默认、设置页推荐同步切换；旧配置中的 yolov8n 自动迁移为 yolo11n（手动选过其他模型的不变）<br>- 安装向导「数据存放位置」补充说明：升级/重装即使显示默认也不覆盖已有数据目录<br><br>v1.3.0<br>- 录像页重构：时间轴下的列表由「录像分段」改为「当日事件」，点击事件跳转到对应时刻播放（无事件时回退显示分段）；含事件的录像段在时间轴上按事件类型着色，并在精确时刻打竖线刻度；导航栏移除「事件」页（路由保留，直达链接仍可用）<br>- 导航栏摄像机与录像的图标互换（标签栏与侧边栏）<br>- 设置页：账户安全与用户管理合并为一组（含组内小标题）；用户管理编辑框支持改用户名（服务端/网页/CLI/向导四端已实装，API 端到端验证通过）<br>- 设置页循环覆盖快捷档位重做：4 列胶囊网格 + 选中填色投影，数值输入改为圆角胶囊样式<br>- AI 推理后端支持 NVIDIA GPU：实测组合 onnxruntime-gpu==1.18.1 + CUDA11 运行库 + cuDNN 8.9.6 + numpy1.26（Tesla P4 / Pascal 可用 CUDA 加速），安装检查提示给出完整组合<br><br>v1.2.0<br>- 修复「推理后端为未知」：AI 检测 socket 路径大小写不一致（启动用 cyanNVR、代理用 cyannvr）导致接口永远连不上，已统一为 /tmp/cyannvr-ai.sock；安装时检查 Python 依赖（cv2/onnxruntime），设置页连接失败时显示原因并每 5 秒自动重试<br>- 循环覆盖：保留天数放开到 3650 天（数字输入 + 30/90/180/365/730/1825/3650 快捷档，替换原 180 天滑条）<br>- 新增录像总容量限额（GB，0 = 不限制），超出后跨摄像头优先删除最旧录像<br>- 摄像头编辑新增单摄限额：保留天数（0 = 跟随全局）与容量限额（GB，0 = 不单独限制）<br>- 支持改用户名：应用设置页「管理员改名」、网页用户管理编辑框放开用户名、CLI 新增 cyannvr rename-user<br><br>v1.1.1-2<br>- 修复重装应用时数据目录 / 端口 / 管理员账号被安装向导默认值覆盖（重装后密码失效、重置码写错目录的根因）<br>- 「应用文件」加入数据目录软链：重置码、录像、事件、日志可在飞牛文件管理中直接查看<br><br>v1.1.1<br>- 应用图标全面升级：深青渐变背景 + 与系统一致的 25.4% 圆角（应用中心 / 桌面入口 / 网页图标全套）<br>- 桌面入口图标文件更名（logo_*）以刷新图标缓存<br>- 设置页重构：移除对已有数据无效的「重置管理员密码」，新增「生成重置码文件」，并展示当前用户名列表<br>- 修复向导自定义管理员名只在首次建库生效、导致每次启动报 UNIQUE constraint 的问题（现仅空库初始化）<br>- 修复 fnOS 启动环境缺 PATH 导致 ffmpeg / python3 找不到（录像与 AI 识别不可用）<br>- 密码重置的「用户不存在」提示补充用户名查看指引<br><br>v1.1.0<br>- 全新应用图标（镜头 + 录制指示）<br>- 新增「忘记密码」重置流程：重置码 5 分钟有效，登录页带倒计时<br>- 新增命令行密码重置：cyannvr reset-password / list-users<br>- 改密后旧登录会话立即失效<br>- 应用中心设置页可修改数据目录、服务端口与管理员密码<br>- 安装向导支持自定义管理员用户名与数据目录<br>- 修复配置变更 / 卸载时无法停止服务<br>- 修复事件缩略图无法加载（401）<br>- 修复录像页进度条无法拖动<br>- 修复导航栏事件图标缺失
