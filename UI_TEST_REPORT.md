# 界面与回放测试记录

测试日期：2026-09-25。代码已快进同步至 `9918359`，原有 Docker 改动保留，原始备份仍在 `codex-before-ui-sync-20260925` stash 中。本次界面修改尚未提交或推送。

## 已完成的修改

- 开启关怀模式隐藏字号选项，关闭后恢复原来的字号选择；普通大字模式只缩放文字，保持布局断点一致。
- 桌面选择器居中显示，明确突出选中项；支持鼠标滚轮、按住拖动和键盘上下键，取消不应用修改。修复定时录像选择器确认参数错误。
- PC 设置页按显示/通知/账户与存储/录像/AI 分列，窄窗口回到单列，保存按钮保持可达。
- 增大关怀模式事件筛选、日历、开关和播放控件的操作区域，修复事件文字与删除按钮重叠、320px 日历溢出、选择器内容裁切。
- 时间轴显示不再错误叠加时区；支持键盘定位、按秒跳转和前后 30 秒操作；分段起止时间显示秒数。
- 回放处理录像空档、跨段续播、切换日期时的异步响应、离开缓存页面时的会话释放；新会话保留暂停和倍速状态。
- 空结果时仍能修改/清除事件筛选；演示数据按设备和日期筛选；设置保存失败不会显示成功。

## 验证结果

| 检查 | 结果 |
|---|---|
| 前端 TypeScript 检查和生产构建 | 通过 |
| 后端编译 | 通过 |
| 320 / 390 / 900 / 1440px，普通 / 特大字 / 深色关怀 / 浅色关怀，6 个页面 | 96 组；被测控件无水平越界，未捕获页面异常 |
| 选择器、关怀开关、字号恢复、键盘、筛选等交互断言 | 19 项通过 |
| 独立 Go 服务 + FFmpeg + 两段真实 H.264 MP4 回放 | 9 项通过 |
| 延迟响应测试 | 切换日期后旧会话未覆盖新页面，旧会话已停止 |

真实回放覆盖解码、暂停、按秒跳转、4 倍速、空档定位并保持暂停、自动续播、切页释放会话、保存失败提示、迟到会话回收。使用本地新建测试数据库，没有修改原有摄像头或录像数据。

## 证据与限制

- PC 设置页：[截图](ui-test/screens/final/1440-care-light-settings.png)
- 桌面选择器：[截图](ui-test/desktop-picker.png)
- 真实录像回放：[截图](ui-test/real-playback.png)
- 布局数据：[96 组测量](ui-test/screens/final/audit.json)
- 回放数据：[会话请求与回收记录](ui-test/real-playback-results.json)
- 可重复脚本与前提：[web/tests/README.md](web/tests/README.md)

测试浏览器为 Windows Edge；小屏检查使用模拟视口。未覆盖实体手机、真实 RTSP 摄像头、H.265 与硬件转码。构建保留原有 HLS 资源包超过 500kB 的提示。
